# SQLite database for Context Studio metadata (PR state, attribution, blast radius).
DATABASE_URL="file:./prisma/dev.db"

# Port for the abstracted version-control backend.
PORT=4000

# Where the authoritative on-disk Git repo of context content lives.
CONTEXT_REPO_DIR=".context-repo"
